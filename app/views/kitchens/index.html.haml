%h1.text-3xl.md:text-5xl.pb-4
  Konyhák
= button_tag "Example", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
%table.mt-4
  %thead
    %tr
      %th Floor
      %th{:colspan => "3"}
  %tbody
    - @kitchens.each do |kitchen|
      %tr
        %td= kitchen.floor
        %td= link_to 'Show', kitchen
        %td= link_to 'Edit', edit_kitchen_path(kitchen)
        %td= link_to 'Destroy', kitchen, method: :delete, data: { confirm: 'Are you sure?' }
.my-3.text-right
  = link_to 'New Kitchen', new_kitchen_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
