- content_for :extra_buttons do
  = link_to 'Eltávolítás', @kitchen, type: :button, method: :delete, data: { confirm: 'Are you sure?' }, class: "bg-red-600 hover:bg-red-500 text-white font-bold py-2 px-4 rounded float-right"

%h1.text-xl.md:text-2xl Konyha szerkesztése
= render 'form', kitchen: @kitchen
.mt-12
  = link_to 'Vissza', @kitchen, class: "bg-blue-600 hover:bg-blue-500 text-white font-bold py-2 px-4 rounded"
