.flex.flex-wrap
  .flex-none.pr-2
    %h2.text-2xl.md:text-4xl.w-full
      %strong= "#{@kitchen.floor}. szinti konyha"
  .flex-auto.mb-4
    .text-right.py-2
      = link_to 'Módosítás', edit_kitchen_path(@kitchen), class: "bg-blue-600 hover:bg-blue-500 text-white font-bold py-2 px-4 rounded"

.mb-4.text-lg.md:text-xl
  A szint eszközei:

.w-full
  - @kitchen.appliances.each do |appliance|
    - color = appliance.status == "operational" ? "green" : "red"
    = link_to appliance, class: "flex flex-wrap items-center cursor-pointer my-1 text-white shadow hover:bg-"+color+"-500 bg-"+color+"-600 rounded " do
      .flex-auto.p-3.break-words
        %span.md:text-lg
          %strong
            = appliance.name
        - if appliance.category == "extra"
          %span.text-xs.font-medium.bg-yellow-200.text-gray-800.py-1.px-2.rounded.align-top
            extra!
      .flex-auto.p-3.text-right
        = appliance.status

.mt-10
  = link_to 'Vissza', kitchens_path, class: "bg-blue-600 hover:bg-blue-500 text-white font-bold py-2 px-4 rounded"
