%h1 Listing users

%table
  %thead
    %tr
      %th Uid
      %th Email
      %th Name
      %th Floor
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        -#%td= user.uid
        %td= user.email
        %td= user.name
        %td= user.floor
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)

%br
